<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═════════════════════════════════════════════════════════ -->
    <!-- ObsidianX Theme — Magenta Nebula (palette tuned to logo)  -->
    <!-- Primary: hot magenta glow · Secondary: electric purple     -->
    <!-- Background: deep purple-black space                        -->
    <!-- ═════════════════════════════════════════════════════════ -->

    <!-- Color Palette -->
    <Color x:Key="DeepSpaceColor">#FF0A0510</Color>
    <Color x:Key="DarkPanelColor">#FF170A28</Color>
    <Color x:Key="SurfaceColor">#FF1F1236</Color>
    <Color x:Key="SurfaceLightColor">#FF2D1A4A</Color>
    <!-- Historically "NeonCyan" — now the primary magenta glow that matches the logo -->
    <Color x:Key="NeonCyanColor">#FFFF2E94</Color>
    <Color x:Key="ElectricPurpleColor">#FFB044FF</Color>
    <Color x:Key="NeonPinkColor">#FFFF6BB0</Color>
    <Color x:Key="NeonGreenColor">#FF00FF88</Color>
    <Color x:Key="TextPrimaryColor">#FFF5E5FA</Color>
    <Color x:Key="TextSecondaryColor">#FFC3A7D8</Color>
    <Color x:Key="TextMutedColor">#FF6F5287</Color>
    <Color x:Key="DangerColor">#FFFF5566</Color>
    <Color x:Key="SuccessColor">#FF00FF88</Color>
    <!-- Dedicated logo-accent colors for direct references in XAML/code -->
    <Color x:Key="LogoMagentaColor">#FFFF1E8A</Color>
    <Color x:Key="LogoRoseColor">#FFFF5FA6</Color>
    <Color x:Key="LogoVioletColor">#FF8A2BE2</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="DeepSpaceBrush" Color="{StaticResource DeepSpaceColor}"/>
    <SolidColorBrush x:Key="DarkPanelBrush" Color="{StaticResource DarkPanelColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource SurfaceLightColor}"/>
    <SolidColorBrush x:Key="NeonCyanBrush" Color="{StaticResource NeonCyanColor}"/>
    <SolidColorBrush x:Key="ElectricPurpleBrush" Color="{StaticResource ElectricPurpleColor}"/>
    <SolidColorBrush x:Key="NeonPinkBrush" Color="{StaticResource NeonPinkColor}"/>
    <SolidColorBrush x:Key="NeonGreenBrush" Color="{StaticResource NeonGreenColor}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>

    <!-- Gradient Brushes -->
    <LinearGradientBrush x:Key="CyanPurpleGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource NeonCyanColor}" Offset="0"/>
        <GradientStop Color="{StaticResource ElectricPurpleColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PurplePinkGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource ElectricPurpleColor}" Offset="0"/>
        <GradientStop Color="{StaticResource NeonPinkColor}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="SidebarGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF12071F" Offset="0"/>
        <GradientStop Color="#FF070310" Offset="1"/>
    </LinearGradientBrush>

    <!-- Magenta → purple logo gradient, used on the top brand strip, filled
         buttons, and the sidebar brain halo. -->
    <LinearGradientBrush x:Key="LogoGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource LogoMagentaColor}" Offset="0"/>
        <GradientStop Color="{StaticResource LogoVioletColor}" Offset="1"/>
    </LinearGradientBrush>

    <!-- Glow Effects — all tuned to the logo's magenta signature -->
    <DropShadowEffect x:Key="CyanGlow" Color="{StaticResource NeonCyanColor}" BlurRadius="18" ShadowDepth="0" Opacity="0.75"/>
    <DropShadowEffect x:Key="PurpleGlow" Color="{StaticResource ElectricPurpleColor}" BlurRadius="18" ShadowDepth="0" Opacity="0.7"/>
    <DropShadowEffect x:Key="PinkGlow" Color="{StaticResource NeonPinkColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
    <DropShadowEffect x:Key="SubtleGlow" Color="{StaticResource NeonCyanColor}" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
    <DropShadowEffect x:Key="LogoGlow" Color="{StaticResource LogoMagentaColor}" BlurRadius="28" ShadowDepth="0" Opacity="0.85"/>

    <!-- Fonts -->
    <FontFamily x:Key="MainFont">Segoe UI, Arial</FontFamily>
    <FontFamily x:Key="MonoFont">Cascadia Code, Consolas</FontFamily>

    <!-- Nav Button Style -->
    <Style x:Key="NavButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,14"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            CornerRadius="8" Margin="6,2" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Active Nav Button -->
    <Style x:Key="NavButtonActive" TargetType="Button" BasedOn="{StaticResource NavButton}">
        <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{StaticResource SurfaceBrush}"
                            CornerRadius="8" Margin="6,2" Padding="{TemplateBinding Padding}"
                            BorderBrush="{StaticResource NeonCyanBrush}" BorderThickness="0,0,0,0">
                        <Grid>
                            <Rectangle Width="3" HorizontalAlignment="Left" Margin="-10,0,0,0"
                                       RadiusX="1.5" RadiusY="1.5">
                                <Rectangle.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="{StaticResource NeonCyanColor}" Offset="0"/>
                                        <GradientStop Color="{StaticResource ElectricPurpleColor}" Offset="1"/>
                                    </LinearGradientBrush>
                                </Rectangle.Fill>
                            </Rectangle>
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Neon Button Style -->
    <Style x:Key="NeonButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="20,10"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}"
                            Effect="{StaticResource SubtleGlow}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource NeonCyanColor}" Opacity="0.15"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="Bd" Property="Effect" Value="{StaticResource CyanGlow}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource NeonCyanColor}" Opacity="0.25"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Filled Neon Button -->
    <Style x:Key="NeonButtonFilled" TargetType="Button" BasedOn="{StaticResource NeonButton}">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource NeonCyanColor}" Offset="0"/>
                    <GradientStop Color="{StaticResource ElectricPurpleColor}" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="#FF0B0B1A"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- Card Panel Style -->
    <Style x:Key="CardPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource DarkPanelBrush}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="BorderBrush">
            <Setter.Value>
                <SolidColorBrush Color="{StaticResource NeonCyanColor}" Opacity="0.1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Stat Text Styles -->
    <Style x:Key="StatNumber" TargetType="TextBlock">
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
    </Style>

    <Style x:Key="StatLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <!-- Text is already uppercase in XAML content -->
    </Style>

    <!-- ═══════════════════════════════════════════════════════════
         ScrollBar — thin (8px), rounded magenta thumb, fades in on hover.
         Custom template because the stock WPF ScrollBar is 17px wide and
         doesn't take a Width setter unless the template is replaced.
         ═══════════════════════════════════════════════════════════ -->
    <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Tb" CornerRadius="3"
                            Background="{StaticResource LogoGradient}"
                            Opacity="0.55" Margin="2,0"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Tb" Property="Opacity" Value="0.95"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Tb" Property="Opacity" Value="1.0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumbHorizontalStyle" TargetType="Thumb" BasedOn="{StaticResource ScrollBarThumbStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="Tb" CornerRadius="3"
                            Background="{StaticResource LogoGradient}"
                            Opacity="0.55" Margin="0,2"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Tb" Property="Opacity" Value="0.95"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Tb" Property="Opacity" Value="1.0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Vertical template — thumb hugs the right edge of scrolling content. -->
    <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
        <Grid Background="Transparent">
            <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Background="Transparent" BorderThickness="0"
                                  Command="ScrollBar.PageDownCommand" Opacity="0"/>
                </Track.IncreaseRepeatButton>
                <Track.DecreaseRepeatButton>
                    <RepeatButton Background="Transparent" BorderThickness="0"
                                  Command="ScrollBar.PageUpCommand" Opacity="0"/>
                </Track.DecreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <!-- Horizontal template — thumb hugs the bottom edge. -->
    <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="ScrollBar">
        <Grid Background="Transparent">
            <Track x:Name="PART_Track" IsDirectionReversed="False" Focusable="False">
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumbHorizontalStyle}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Background="Transparent" BorderThickness="0"
                                  Command="ScrollBar.PageRightCommand" Opacity="0"/>
                </Track.IncreaseRepeatButton>
                <Track.DecreaseRepeatButton>
                    <RepeatButton Background="Transparent" BorderThickness="0"
                                  Command="ScrollBar.PageLeftCommand" Opacity="0"/>
                </Track.DecreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="8"/>
                <Setter Property="Height" Value="Auto"/>
                <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto"/>
                <Setter Property="Height" Value="8"/>
                <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- TextBox Style -->
    <Style x:Key="NeonTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="CaretBrush" Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource ElectricPurpleBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource NeonCyanBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Dark ComboBox with fully custom template — the stock WPF template
         renders the SELECTED item through a ContentPresenter that falls
         back to SystemColors.WindowBrushKey (white) regardless of our
         Foreground setter. Overriding the template is the only reliable
         fix to make the current value readable on the cyberpunk theme. -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource DarkPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="22"/>
                        </Grid.ColumnDefinitions>

                        <!-- Outer box -->
                        <Border x:Name="OuterBorder" Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"/>

                        <!-- Currently-selected item (the bit that was going white) -->
                        <ContentPresenter x:Name="SelectionBox" Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" HorizontalAlignment="Stretch"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          IsHitTestVisible="False"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          TextElement.FontFamily="{TemplateBinding FontFamily}"
                                          TextElement.FontSize="{TemplateBinding FontSize}"/>

                        <!-- Dropdown arrow -->
                        <Path Grid.Column="1" Data="M 0,0 L 4,5 L 8,0 Z"
                              Fill="{StaticResource NeonCyanBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Width="8" Height="5" IsHitTestVisible="False"/>

                        <!-- Clickable toggle -->
                        <ToggleButton Grid.ColumnSpan="2" Background="Transparent"
                                      BorderThickness="0" Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <!-- Dropdown popup -->
                        <Popup Placement="Bottom" AllowsTransparency="True"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               PopupAnimation="Slide" Focusable="False">
                            <Border Background="{StaticResource DarkPanelBrush}"
                                    BorderBrush="{StaticResource NeonCyanBrush}"
                                    BorderThickness="1" CornerRadius="3"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border.Effect>
                                    <DropShadowEffect Color="{StaticResource NeonCyanColor}"
                                                      BlurRadius="12" Opacity="0.3" ShadowDepth="0"/>
                                </Border.Effect>
                                <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter SnapsToDevicePixels="True"/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush"
                                    Value="{StaticResource NeonCyanBrush}"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush"
                                    Value="{StaticResource NeonCyanBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{StaticResource DarkPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
